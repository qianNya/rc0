name: rc0_ui
description: rc0 Liquid Glass design system (theme/glass migration in progress).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  rc0_core:
    path: ../rc0_core

dev_dependencies:
  flutter_lints: ^6.0.0
