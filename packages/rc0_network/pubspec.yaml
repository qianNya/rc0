name: rc0_network
description: rc0 HTTP kernel — REST envelope parsing and auth header helpers.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  rc0_core:
    path: ../rc0_core
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
