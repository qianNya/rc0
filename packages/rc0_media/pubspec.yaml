name: rc0_media
description: rc0 media kernel — ImageRef resolution, URL utils, LocalMediaFile.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  rc0_core:
    path: ../rc0_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
