---
description: 响应式断点、动效连续性与 Liquid Glass 性能红线，写任何 Dart UI 代码时适用
globs: lib/**/*.dart
alwaysApply: false
---

# 响应式

断点以 `Breakpoints`（`lib/core/responsive/breakpoints.dart`）为准，不写 magic width：

- compact 600：单列，底部浮动导航。
- medium 840：侧栏壳切换，内容双列。
- expanded 1024：桌面三栏（导航/内容/检查器）。

同一业务页面在各端保持同一心智模型，只改布局不改流程。

# 动效连续性

- Tab/导航/面板切换必须动画化，不允许瞬时跳变。
- 结构增删（如编辑器大纲）需 `AnimatedSize` / `AnimatedSwitcher` 过渡。
- 默认时长优先 `AppMotion.fast|normal`，不超过 400ms。
- 尊重系统"减弱动态效果"：液态动效降级为交叉淡化，不降级功能。

# 性能红线

- 同屏 blur 层 ≤ 2；Glass 层复用共享组件，禁止新建独立 `BackdropFilter`。
- 图片一律走 `Rc0Image`（缓存 + WebP），禁止裸 `Image.network`。
- 大列表分页/懒加载；避免深层嵌套滚动。
- 状态变化局部化（`ListenableBuilder`/provider 精确订阅），减少整页 rebuild。
- 页面切换动画掉帧 = 缺陷。

# 深度层级

保持 Depth 关系：内容(0) < 导航(1) < dialog(2) < sheet(3) < critical(4)。

不得让工具层在视觉上压制内容层。
