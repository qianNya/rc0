---
description: 测试结构与编写规范
globs: "test/**"
alwaysApply: false
---

# 测试规范（Testing）

## 结构

`test/` 镜像 `lib/` 路径，import 用 `package:rc0/...`。

```
test/core/utils/image_url_utils_test.dart
test/api/api_headers_test.dart
test/widget_test.dart
```

## Widget 测试

```dart
TestWidgetsFlutterBinding.ensureInitialized();
SharedPreferences.setMockInitialValues({});
```

## 单元测试

- `group()` 分组；优先覆盖 utils、network 适配层、Repository 逻辑
- 新 Repository 覆盖 happy path + error path
- 不强制 golden / integration test

参照：`test/api/api_headers_test.dart`、`test/core/utils/image_url_utils_test.dart`
