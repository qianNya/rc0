---
description: UI/UX 设计系统与组件复用规范
globs: "lib/app/theme/**,lib/shared/widgets/**,lib/features/**/presentation/**"
alwaysApply: false
---

# UI 设计系统（Design System）

Material 3 风格，支持 Light/Dark，间距基准 4/8/16/24/32。

## Design Tokens（必须使用）

| Token | 文件 | 用途 |
|-------|------|------|
| `AppColors` | `app_colors.dart` | 颜色（暗色用 `*Dark`） |
| `AppDimensions` | `app_dimensions.dart` | 间距、圆角、`pagePadding` |
| `AppTextStyles` | `app_text_styles.dart` | `display` / `title` / `body` / `label` |
| `AppShadows` | `app_shadows.dart` | 卡片、底部导航阴影 |

```dart
// ❌ BAD
color: Color(0xFF6B4FE0)
padding: EdgeInsets.all(12)

// ✅ GOOD
color: AppColors.accent
padding: const EdgeInsets.all(AppDimensions.spacingMd)
style: AppTextStyles.body
```

暗色模式通过 `ThemeModeNotifier` + `AppTheme`，文字颜色暗色场景用 `*Dark` token 或 `Theme.of(context)`。

## 布局与组件

- 页面壳：`AppScaffold` / `AppCard`
- 响应式：`Breakpoints`（≥1024px 桌面）+ `ResponsiveBuilder`
- 桌面：`adaptive_shell_page` + `desktop_title_bar`
- 图片预览：复用 `image_preview.dart`
- 动画：优先隐式动画，避免重量级自定义动画

## 交互要求

列表/数据页必须有 **空状态**、**加载中**、**错误** UI。UI 不直接调用 API，不暴露 raw DTO。
