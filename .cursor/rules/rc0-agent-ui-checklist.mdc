---
description: RC0 Liquid Glass 2.0 UI 执行清单
alwaysApply: true
---

# Agent 执行清单（强制）

1. 内容优先：先看内容层，再放导航与控制层。
2. 导航/Tab/搜索必须浮动，禁止贴边实心条。
3. 圆角使用连续曲率（24/28/32/Pill），避免 4/6/8。
4. 透明优先于不透明；UI 不得遮蔽主要内容。
5. 所有进入/退出/切换操作必须有动效（fade/slide/scale/blend）。
6. Tab 内容变更（增删改、展开收起）必须动画过渡。
7. 仅使用 `Glass*` / `Rc0AppBar` / `AppBottomNavBar` 等共享封装。
8. 颜色/间距/圆角/阴影/动画全部使用 token。
9. 提示弹出（SnackBar/Toast/反馈条）必须走统一全局反馈样式。
10. 页面必须具备空/加载/错误三态；数据经 Repository。
11. 若页面更像 Material 设置页/后台面板，立即重构。

通过标准：更接近 Apple Music / visionOS 空间体验，界面退后、内容主导。
