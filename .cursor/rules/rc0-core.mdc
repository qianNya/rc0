---
description: rc0 项目全局约定、目录边界与禁止事项
alwaysApply: true
---

# rc0 核心约定（Core）

本地剧本（Screenplay → Act → Scene → Frame）管理与浏览，Flutter 多平台。

## 目录地图

| 目录 | 职责 |
|------|------|
| `lib/app/` | Theme、go_router、导航工具 |
| `lib/core/` | domain、network、responsive、utils |
| `lib/features/` | Feature-first（`data/` + `presentation/`） |
| `lib/shared/` | 跨功能 UI 与服务 |
| `lib/api/` | 手写 Rust 对齐 HTTP 客户端（`api/data/vars`） |

## 技术栈（实际使用）

- **状态**：Singleton `ChangeNotifier` Repository + `addListener` + `scheduleSetState`
- **路由**：`go_router` + `AppRoutes`
- **设计**：`AppColors` / `AppDimensions` / `AppTextStyles` / `AppShadows`
- **Lint**：`flutter_lints`（`analysis_options.yaml`）

## 禁止事项

- 不引入 Provider / Riverpod / Bloc / GetX
- 不使用 dio / freezed / hive / drift（项目未采用）
- 不修改 `lib/api/**` 时需与 `rc0-rust/docs` 保持路径一致；后端变更后同步更新手写客户端
- 不硬编码颜色、间距、路由路径
- UI 不直接调用 API（经 Repository）

## 参考文档

`README.md` · `lib/api/API_STRUCTURE.md` · `docs/APP_API_MATRIX.md`
